# Bridge server config.toml example

port = "{{PORT}}"
horizon = "{{BRIDGE_HORIZON_URL}}"
network_passphrase = "Test SDF Network ; September 2015"
api_key = "{{BRIDGE_API_KEY}}"

[database]
type = "postgres"
url = "{{DATABASE_URL}}"
